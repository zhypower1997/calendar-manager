version: '3'

services:
  calendar-manager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    volumes:
      - ./data:/mnt/workspace
    environment:
      - NODE_ENV=production
    restart: unless-stopped